 # if re.match(r'^\d+', line):
                #     file_size = (f'File Size : {line.split(' ')[0]}')
                #     file_name = (f'File Size : {line.split(' ')[1]}')
    

                #     # Add all values to the 'goal' list
                #     for item in file_direct[level]:
                #         if direct in item:
                #             item[direct].extend(file_size)
                #             item[direct].extend(file_name)